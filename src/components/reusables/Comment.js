import React, { Component } from 'react';
import { View, StyleSheet, Text } from 'react-native';

export default class Comment extends Component {
  render(){
    const { author, username, text } = this.props;
    return (
      <View>
      </View>
    )
  }
}

const styles = StyleSheet.create({

});